#include "../include/utils.h"

// This file is intentionally left blank for now.
// All template class implementations (like ThreadSafeQueue)
// must be in the header file (utils.h).
//
// We keep this file in the project in case we want to add
// non-template utility functions in the future.