services:
  - type: web
    name: apexmatch-engine
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
